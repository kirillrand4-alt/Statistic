# Правила сопоставления (копим итеративно)

## Принцип
Сопоставляем ПО ССЫЛКАМ. Якорь — `prokompressor.ru`. Для каждого товара prokompressor
ищем у конкурентов строку с тем же «отпечатком» URL и берём цену.

## Извлечение отпечатка из URL
- Берём **последний сегмент** пути (слаг товара) + **бренд ищем по всему пути**
  (у части сайтов бренд — отдельный сегмент, напр. compressortyt: `/hansmann/rs15e-10-bar/`).
- Токенизируем по `- _ / . пробел`.
- Отпечаток = `бренд :: отсортированный набор значимых токенов модели`.

## ШУМ (выкидываем)
- Тип товара: vintovoy, porshnevoy, kompressor(y/i), elektricheskie, ...
- Служебное в пути: katalog, produkcii, stanciya, kompr, shop, oborudovanie, catalog
- Единицы-слова: `bar`/`бар`, `l`/`л`, `kvt`/`kw`  (но ЧИСЛО рядом — сигнал!)
- Слово бренда (выносим в отдельное поле)
- `IP23` — это значение по умолчанию → шум

## СИГНАЛ (сохраняем дословно)
- Ядро модели: `rs15e` ≠ `rs15a` (одна буква = другой товар и цена)
- Число давления: `-10` / `-8` / `-12` / `-13` (разные позиции)
- Значимые довески: `ip54`, `500`, `dr`, `chastotnik` (частотник) и т.п.

## Нормализация написания
- `ip-54` / `ip 54` → `ip54`
- `500dr` / `500 dr` / `500_dr` → `500` + `dr`
- разделители `, . - _` → единый

## Дедупликация
- Одинаковый URL встречается несколько раз (парсинг в разные дни) → берём **минимальную цену**.

## Проверено экспертом (батч №1, Hansmann)
- ✅ RS15E 10 = один товар across 5 сайтов (`bar`=шум, давление=сигнал)
- ✅ RS15A 10 = один товар across 5 сайтов
- ✅ `rs15e` ≠ `rs15a` — разные товары (ядро модели = сигнал)
- ✅ `IP54` корректно отделён от обычного исполнения
- ✅ `500DR` корректно отделён (большой ресивер+осушитель)

## Проверено экспертом (батч №1, продолжение)
- ✅ `rs15a-10` ≠ `rsa-15-10` — РАЗНЫЕ товары (эксперт подтвердил). Матчер держит раздельно — верно. Нормализацию `RS{N}A↔RSA{N}` НЕ добавляем.

## Открытые вопросы (на проверку у эксперта)
- `IP23` всегда дефолт? на всех брендах?
