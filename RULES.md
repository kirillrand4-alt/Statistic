# Правила сопоставления (копим итеративно)

## Принцип
Сопоставляем ПО ССЫЛКАМ (URL надёжнее названий — названия местами испорчены Excel,
напр. `1,9/1,0` → дата-сериал `1.46031.0`). Для каждого товара ищем у конкурентов
строку с тем же «умным отпечатком» URL и берём цену. Якорь — `prokompressor.ru`;
дополнительно показываем кластеры, где нас нет, но есть 2+ конкурента (кандидаты добавить).

## Извлечение отпечатка из URL (умный ключ)
- Берём **последний сегмент** пути (слаг) + **бренд ищем по всему пути**.
- Токенизируем по `- _ / . пробел`; `500dr`→`500,dr`; `ip-54`→`ip54`.
- Стоп-слова фильтруем **ДО** разбиения (чтобы `10s` сохранил вариант `s`, а предлог `s`=«с» убрался).
- Ключ = `бренд :: СКЕЛЕТ :: ОДНОЦИФРЫ :: БУКВЫ :: МНОГОЦИФРЫ`:
  - **СКЕЛЕТ** — слова модели как СОРТИРОВАННОЕ множество (порядок слов между сайтами
    нестабилен: `peredvizhnoy` в начале/конце, «5 Plus»/«Plus 5»). Маркеров разрыва нет
    (они ложно разъединяли ~456 верных матчей ET/Ariacom/Dalgakiran).
  - **ОДНОЦИФРЫ** — однозначные числа в ИСХОДНОМ порядке (части дробей: `2,5` ≠ `5,2`).
  - **БУКВЫ** — одиночные буквы-варианты, отсортированы (`D VS` = `VS D`, `10S`≠`10`).
  - **МНОГОЦИФРЫ** — ресивер (макс. число, если ≥2× следующего) **плавает**, мощность/давление
    остаются в порядке. Ловит: `240/10`=`10/240` (Ariacom, ресивер плавает),
    `15/300-10`≠`10/300-15` и `15SD-10`≠`10SD-15` (Fiac, близкие мощн./давл. — порядок значим).

## ШУМ (выкидываем)
- Тип товара: vintovoy, porshnevoy, kompressor(y/i), elektricheskie, ...
- Служебное: katalog, produkcii, stanciya, shop, oborudovanie, catalog
- Единицы-слова: `bar`/`бар`, `l`/`л`, `kvt`/`kw`, предлоги `s/na/i/so/dlya`
- Слово бренда (в отдельное поле) · `IP23` — дефолт → шум

## Нормализация / транслит
- URL-декод: `%D0%B2`→`в` (кириллица в слагах prokompressor — вольтаж Ceccato и т.п.).
- **bk→vk**: ВК (винтовой компрессор) транслитерируется и как `vk` (мы), и как `bk` (конкуренты, В→B);
  `bk60`→`vk60`. Безопасно: БК-безмасляные prokompressor спасает слово «безмасляный» (0 ложных слияний).
  Чинит Remeza +44, Berg +56.
- **crossair→cross**: один бренд (эксперт подтвердил); prokompressor — устаревшие серии CA,
  у конкурентов — CrossAir. Пересечение малое (~25), но вскрывает ~185 кандидатов добавить.

## Дедупликация
- Одинаковый URL встречается несколько раз → берём **минимальную цену**.

## Проверено экспертом
- ✅ **Hansmann** (батч №1, построчно): `RS15E 10`=`RS15A 10` различны; `rs15e`≠`rs15a`;
  `IP54` отделён; `500DR` отделён; `rs15a-10`≠`rsa-15-10` (нормализацию НЕ добавляем).
- ✅ **Dalgakiran**: «Plus 5» = «5 Plus» (порядок слов — ОДИН товар, 50 из 51 пар). Случай
  `Plus 75-7` vs `7 Plus 7.5` — это дробь `7.5`→`75` в URL; ловится детектором дроби (жёлтый).
- ✅ **ZIF**: `СВЭ-2,5/0,7` ≠ `СВЭ-5,2/0,7` (порядок цифр дроби) — РАЗНЫЕ.
- ✅ **Ariacom**: `NT3 240/10` = `NT3 10/240` (ресивер плавает) — ОДИН товар.
- ✅ **Fiac New Silver** (батч №2): `D 15/300-10`≠`D 10/300-15`, `D 15SD-10`≠`D 10SD-15`,
  `10/500`≠`10S/500` — РАЗНЫЕ модели (исправлено правилом ресивера + сохранением `s`).

## Детектор дроби (build_review, реализован)
- **Дробь в слаге роняется** некоторыми сайтами: pnevmoteh `ВК-5.5Р`→`vk-55r`, `7.5`→`75`
  (в URL неотличимо от 55/75). Значение живёт только в названии.
- Детектор сравнивает дробь в НАЗВАНИИ нашего товара и сматченного конкурента; если разные
  (и это не Excel-битьё `*.46xxx`) → красит ЖЁЛТЫМ. Только на наших строках. Сейчас ~12 на 11 брендов.
- Названия конкурентов местами испорчены (Excel-даты `1.46031`) — фильтруем, на URL-матч не влияет.

## Открытые вопросы
- `IP23` всегда дефолт на всех брендах?
