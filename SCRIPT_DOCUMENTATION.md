# Спек-матчер: как устроен и как работает

Документация для презентации. Описывает скрипт сопоставления нашего каталога
(prokompressor.ru / Битрикс) против карточек **6 сайтов-конкурентов**:
`compressortyt.ru · aerocompressors.ru · pnevmoteh.ru · pnevmo-sklad.ru · v-p-k.ru · rutector.ru`.

Результат — **по одному Excel-файлу на бренд** (≈51 бренд), собранные в `Brands_spec_match.zip`.

---

## 1. Идея в одном абзаце

Мы не доверяем названиям «в лоб» (они пишутся по-разному на каждом сайте) и не
доверяем ценам без проверки. Сцепка строится на **инварианте товара**: бренд +
серия+номер из названия (обязательно), а дальше характеристики (кВт, бар,
производительность) должны **не противоречить** в пределах инженерного допуска,
плюс жёсткие флаги-варианты (осушитель, частотник, ресивер, IP, охлаждение,
привод). Если на товар нашёлся ровно один кандидат с сайта — это матч; если
несколько разных — «проверь вручную»; если ноль — у нас нет аналога / нет данных.

```
URL/название конкурента ─┐
                         ├─► бренд ─► серия+номер ─► специи не противоречат ─► флаги-варианты ─► МАТЧ
наш товар из Битрикса ───┘            (обязательно)     (кВт/бар/произв)        (FF/VSD/ресивер/IP/охл/привод)
```

---

## 2. Источники данных (входы)

| Что | Файл/источник | Что берём |
|---|---|---|
| Наш каталог | `specs_compact.csv` (выгрузка Битрикса, колонки `IP_PROP*`) | бренд, кВт, бар, л/мин, масло, частотник, ресивер, осушитель, привод, охлаждение, вес |
| Наши цены | `products_export_*.csv` | название, URL, цена (по коду товара) |
| Конкуренты — специи | прогоны парсера (`SCRAPE_FILES`, CSV: `product_url, name, specs(JSON), price, sku, series_status`) | характеристики из спек-таблиц карточек |
| Конкуренты — вселенная URL | большой прайс-файл + sitemap (xlsx) | полный список товарных URL (даже без специй) |

**Объединение прогонов парсера.** Specs склеиваются по URL из всех выгрузок в
хронологическом порядке: **поздний прогон переписывает ключи** (если поле сняли
хоть в одном прогоне — оно снято). Прогоны обновлённого парсера помечены
(`NEW_PARSER_FILES`): если URL пройден новым парсером, а поле всё равно пустое —
значит сайт его не публикует, повторно гонять бессмысленно.

**Ключевые поля Битрикса** (наш каталог):
`22553 бренд · 22562 кВт · 22573 бар · 22571 л/мин · 22583 масло · 22586 частотник ·
22564/22574 ресивер · 22565 осушитель · 22601 привод · 22669 охлаждение · 22555 вес`.

---

## 3. Распознавание бренда (`matcher.py`)

1. **Сначала из URL** (`find_brand`): идём по сегментам пути, ищем известный бренд
   в словаре `BRAND_ALIASES` (синонимы → канон: `dgk→dalgakiran`, `ingersoll/rand→ir`,
   кириллица `зиф→zif`, `чкз→chkz` и т.д.).
2. **Иначе из названия** (`brand_from_text`): длинные имена проверяются раньше
   коротких («atlas copco» раньше «atlas»).
3. **Особый случай `ac`**: «AC» = Atlas Copco **только если** следующий токен —
   серия Atlas («ac ga22»). Иначе «AC» = air-cooled (воздушное охлаждение), не бренд.

Словарь `BRAND_ALIASES` дополняется по мере охвата каталога; в нём учтены и
бренды-конкуренты, которых у нас нет (Xeleron, Denair, Kaishan…), и недавно
добавленные из «тёмной материи» (mikropor, pneumatech, rotair, ats, friulair).

---

## 4. Извлечение серии — ядро сцепки (все приёмы «ловли»)

Серия+номер — **обязательное** условие. Берётся из названия + последнего сегмента
URL (без домена, чтобы «aeroCOMPRESSORs» не дал ложный «compressor»).

### 4.1 Atlas — отдельный regex (`series_num`, `SER_ATLAS`)
Жёсткий список серий (XAHS, XRHS, GA, GX, ZR, ZT, SF, …). **Длинные серии
проверяются раньше коротких** — иначе короткая «x» сожрала бы «xahs». Дополнительно:
- `COPCOG 200` → `COPCO G 200` (вставляем пробел после «copco/копко»);
- суффикс **`L` = low pressure**: `G15L ≠ G15`, `GA37L ≠ GA37`;
- **`+`/`plus` после номера = другая модель**: `GA11 ≠ GA11+`. Плюс ловится в трёх
  позициях: приклеенный к серии (`GA11+`), приклеенный к числу, и **отдельным
  словом после давления** (v-p-k пишет `GA 18 10 plus` = GA18+).

### 4.2 Все остальные бренды — дженерик (`gen_series`)
Первое «слово (2–12 букв) + число» после чистки. Приёмы:

| Приём | Зачем | Пример |
|---|---|---|
| **Кириллица → латиница** (`_CYR2LAT`) | наш каталог пишет серии кириллицей (ЕКО/ВК/КМ), конкуренты — латиницей (EKO/VK/KM); только чистые омофоны `аеокмртхвн→aeokmrtxvn` | `екоcd = ekocd`, `ВК-18.5 = vk18.5` |
| **Склейка дефиса/точки внутри слова** | разное написание | `k-max = kmax`, `dr.sonic = drsonic` |
| **Сохранение десятичных** | не разбить дробь | `i.18,5 → i 18,5` (но `18.5` цело) |
| **Отброс маркетинг-слов** | «new/новый» — не серия | `COMARO MD NEW 55 = MD55` |
| **Стоп-слова и бренд-токены пропускаются** | «винтовой/компрессор/бренд» — не серия | пропускаем до реальной серии |
| **Одиночная латинская буква-вариант** | линейка инвертора и т.п. | `GENESIS I` = инвертор (≠ GENESIS) |
| **Хвост до 2 латинских токенов ПОСЛЕ числа** | заводские sku-варианты (доказано V1-артикулами) | `Ozen OSC 110D/110U/110S`, `Ekomak DMD 100 C/CR/CRD` |
| **`+` приклеенный к числу = часть имени** | `HCA+ ≠ HCA` | `ARIACOM HCA+110` |
| **«plus» сразу после бренда = реальная серия** | не маркетинг | `Fini PLUS 11-08` |
| **FALLBACK: одно-буквенная серия** | срабатывает только если основной проход дал None | `Atom А-11`, `Boge C 10`, `Comprag D-11`, `IR R110` |

В фолбэке кириллические **предлоги/союзы** («с», «и», «в»…) отсекаются, но «а»
остаётся (серия Atom А).

### 4.3 «Отбрасывание хвостов» и хвостовые маркеры
- **Стоп-суффиксы** не попадают в ключ серии: `plus`, `vsd`, `ff`, `pack`, `tm/fm`,
  `ac/wc`, единицы (`l/kw/bar`…). Так `VEGA 11 R PLUS` и `VEGA R PLUS 11` дают
  один ключ, а `SPINN 11-10 FM` и `SPINN 11 FM 10` не расходятся.
- **Хвостовые флаги не-Atlas** (`suffix_flags`): `Д/D` после числа/«л» = осушитель
  (`270L D`, `500Д`); `ВС/bc` = упомянут воздухосборник/ресивер; `/O, /OW, /O2`
  (Zammer) = осушитель.

### 4.4 Berg — заводская схема кодов ВК (`berg_suffix`)
Буквы в коде ВК декодируются (подтверждено пропами нашего каталога):
**Р = ременный привод · Е = частотник (VSD) · О = осушитель** (комбинации РЕ/РО/РЕО).
Буквы либо приклеены к числу (`ВК-18.5РО-500`), либо идут одиночными токенами
сразу после (`ВК-11 Е 10`). Это превратило «монстров» Berg (в среднем 16 пар на
товар) в чистые сцепки (~4.7) — раньше ВК-коды без расшифровки матчились веером.

---

## 5. Числовые характеристики и допуски (`spec_match.py`)

| Характеристика | Допуск | Почему так |
|---|---|---|
| **кВт** | 6% | разброс округлений мощности двигателя |
| **бар** | 3% (строго) | v-p-k пишет рабочее (6.9 при номинале 7) — терпим; но 7 vs 7.5 (6.7%) режем |
| **производительность** | 4% (строго) | `GA11=1560` vs `GA11+=1820` не должны путаться |

**Санитарные диапазоны** (отсев мусора): кВт 0.2…2000; бар 3…400; цена 100 ₽…50 млн ₽.

**Приведение производительности к л/мин** (`flow_to_lmin`): по единице из ключа —
`м³/мин ×1000`, `м³/час ÷0.06`, `л/мин` как есть; без единицы и `<60` трактуем как
м³/мин. Отсев нереального (<100 или >120000 л/мин) — заодно ловит давление,
случайно попавшее в поле произв.

**Диапазоны → максимум.** У VSD производительность и давление пишут диапазоном
(`575–2935 л/мин`, `4–13 бар`). Берём **максимум** = номинал модели (как в каталоге
prokompressor): `bar_value`, `flow_value`.

**Сдвоенные карточки** (`bar_flow_pairs`). Карточка `8/10 бар` + `1.665/1.435 произв`
= **перечень вариантов на одной странице** → разбиваем на пары `(8, 1665)`,
`(10, 1435)`, каждая матчит свой SKU. Признак: `/` = варианты, ` - ` = непрерывный
диапазон. Цена на странице одна → вешаем на **младший** (базовый) вариант, на
старшие не вешаем (была бы занижена).

**`is_flow_key`** — критическое различение: ключ «Производительность, л/мин» — это
производительность, а «Производитель» / «Производство» / «Гарантия производителя» /
«Страна производства» — **нет** (все ловятся подстрокой «произв»). Этот баг прятал
производительность у тысяч карточек rutector (восстановлено ~3 380, полный паспорт
2 965 → 6 344).

---

## 6. Флаги-варианты (`text_flags` и пропы)

Одна серия+номер может иметь варианты-исполнения, которые сильно влияют на цену.
Они вшиты в артикул/название:

- **FF = осушитель.** Часто приклеен к давлению (`G 11 10FF`, `ga18plus-8-5ff`).
- **VSD = частотник** (важнейший фактор: **+до 35% к цене**). Маркеры:
  `vsd / частот / инвертор / inverter / VS (ET) / I-серии ABAC (genesis i.15,
  formula.i) / MEI`. Заводской код в имени надёжнее спек-таблицы (там «частота тока/
  вращения» ≠ частотный привод).
- **Ресивер.** `ресивер/resiver/TM + число`; хвостовые объёмы `270/500/900`;
  `100/150/200/300` — только в хвостовой позиции (иначе спутать с номером модели,
  `GA 200`).
- **IP-класс** (`ip_class`): `54 ≈ 55` (один ценовой класс, нормализуем 55→54),
  `23` — другой (открытое исполнение).
- **Охлаждение** (`cool_class`): water/air из пропа + спек-ключа + имени. Если в
  значении ОБА («воздушное/водяное, опционально») → None (подходит к любому).
  Голый `ac` не трактуем (= Atlas Copco).
- **Привод** (`dr`): ременной/прямой — наш из пропа Битрикса, конкурента из спеки
  или схемы Berg.

### Тристейт для да/нет-полей (частотник, осушитель)
Принципиально: **«нет данных» ≠ «нет»**.
- `1` = знаем-да (имя-маркер > проп 22586 > спек-ключ);
- `0` = знаем-нет (проп/спека явно «нет»);
- `None` = не указано — **матчится**, но подсвечивается особой пометкой.

---

## 7. Направленные фильтры: «молчание = совместимо»

Главный принцип всех фильтров — **режем кандидата только при ЯВНОМ конфликте**;
если одна из сторон молчит, это не противоречие. Применяются цепочкой после `match`:

```
match → ip_filter → cool_filter → ff_filter → receiver_filter
```

- **`ip_filter` / `cool_filter`**: отбрасываем, только если обе стороны размечены и
  классы различаются.
- **`ff_filter`** (осушитель): если в серии конкурент *размечает* FF (есть карточки
  с ff=1) — молчуны при нашем FF отбрасываются (раз сосед подписан, молчание = без
  осушителя); если FF не пишет никто (Atlas ZR/ZT) — молчание не противоречит.
- **`receiver_filter`**: наш артикул всегда кодирует вариант. Нет ресивера у нас →
  кандидаты с явным ресивером убираем; есть → молчунов отбрасываем при наличии
  явных, объёмы (если оба известны) должны сойтись.
- **Частотник/привод** — внутри `match`: режем только явный конфликт `1 vs 0`;
  привод сравниваем, лишь когда известен с обеих сторон.

---

## 8. Правило сцепки `match()` — итог

Кандидат проходит, если:
1. **серия+номер совпали** (обязательно);
2. кВт ±6%, бар ±3%, произв ±4% — где значения есть с обеих сторон;
3. **частотник** не в явном конфликте (1≠0);
4. **привод** не в конфликте (если известен с обеих сторон);
5. **осушитель** не в конфликте;
6. **ресивер** не в конфликте.

**Масло НЕ сравниваем**: внутри одной серии+номер масляность не варьируется, а
конфликт = враньё сайта (`compressortyt`: XAS97 помечен «безмасляный» на масляном).

После `match` — направленные фильтры (раздел 7). Затем результат раскладывается:
- **1 кандидат с сайта** → спек-матч;
- **2–3 разных** → «неоднозначные» (жёлтый, проверить);
- разные *исполнения одной физ-спеки* (AC/WC/Pack/фаза) сворачиваются в **один
  товар** (жёлтая ячейка, цена = min).

---

## 9. Структура Excel-файла: листы, колонки, цвета, пометки

Каждый брендовый файл — до **6 листов**:

### Лист 1 «спек-матч» (главный) и Лист 2 «неоднозначные»
Колонки: `№ · Наш товар · Ваша цена · [6 сайтов] · min конк. · Δ к min, % ·
Почему сцепилось · Проверить карточку · ВЕРДИКТ`.
- В ячейке сайта — **цена** кандидата (мин. среди живых), кликабельна.
- **«Неоднозначные»** = там, где хотя бы у одного сайта нашлось >1 разной модели.

### Лист 3 «GAP — нет у нас»
Модели, которые есть у **≥2 конкурентов**, а у нас серии нет. Это поиск дыр в
ассортименте. Колонки: `Модель · Серия · кВт · бар · Сайтов · [6 сайтов] · min`.
Возд./вод. охлаждение считаются разными товарами (не схлопываются).

### Лист 4 «Проверить карточку»
Наша характеристика **расходится** с конкурентами той же модели (`card_issue`).
Проверка идёт по полю: пиннуем остальные поля + FF/VSD; если наше значение не
подтверждает никто, а на другом значении сходятся **≥2 сайта** — флаг. Так
вскрывается ×10-ошибка (Atmos ST45: 680 vs 6600 л/мин) и не флагуется верное
значение, подтверждённое карточкой.

### Лист 5 «Снятые у конкурентов»
Карточки конкурентов со статусом «снято с производства» + наш товар того же ряда.

### Лист 6 «Особо проверить (нет серии)» (power-only)
Товары **без серии в названии** (Airpol 11/10, ZUV 11, MIG 11). Серии нет, поэтому
правило строже: бренд + **все три** специи равны (fl ±2%) + vsd/ff строго + IP
равен + привод из текста строго + вариант-токены имён совпадают + receiver-фильтр.

### Цвета и шрифты
| Оформление | Значение |
|---|---|
| **Синий подчёркнутый** (`0563C1`) | гиперссылка / активная цена |
| **Красный зачёркнутый** (`C00000`, strike) | снято с производства |
| **Жёлтая заливка** (`FFE699`) | свёрнуто несколько карточек/исполнений — проверить |
| **Светло-серая заливка** (`F2F2F2`) | с этого сайта матча нет |
| **Оранжевый** (`C55A11`) | спека под вопросом (ВЕРДИКТ / card_issue) |
| **Персиковая заливка** (`FCE4D6`) | наш товар, у которого спека под вопросом |
| **Бел. жирный на тёмно-синем** (`305496`) | шапка |
| Текст ячейки `По запросу` | цена не найдена (есть карточка, нет цены) |
| Текст ячейки `снято` | снято с производства, цены нет |

### Колонка «Почему сцепилось» — расшифровка матча
Строка вида:
```
GA22 · кВт 22≈22 · бар 8≈8 · произв 3800≈3750 · FF · VSD · ресивер 500≈500 · возд.охл · ⚠ VSD у конкурента не подтверждён
```
- `СЕРИЯ+НОМЕР` (заглавными);
- `кВт/бар/произв X≈Y` — наше≈конкурента (`—` если поля нет);
- `FF` / `VSD` — активные флаги;
- `ресивер X≈Y`, `вод.охл`/`возд.охл`;
- **`⚠`** — особая пометка тристейта: одна сторона ДА, вторая молчит (рисковый
  случай +35% к цене). «Знаем-нет ↔ молчит» не шумим — типовой фикс без частотника.

Колонка **ВЕРДИКТ** дублирует `card_issue` прямо в строке матча (оранжевым,
ссылкой на сайт-источник), если наша спека расходится с конкурентами.

---

## 10. Как запускать

```bash
python brand_spec_review.py            # все бренды → brand_reports/*.xlsx + Brands_spec_match.zip
python brand_spec_review.py atlas      # один бренд (для отладки)
```

Печатает по каждому бренду сводку: `матч / неодн / без / GAP / карточки / снятые /
без серии`.

**Вспомогательные скрипты** (тот же движок извлечения — `atlas_need_specs.py`):
- `atlas_need_specs.py`, `brands_need_specs.py` — списки URL без полного паспорта
  (кВт+бар+произв) для дочистки парсером;
- `categories_need_scan.py` — осушители/ресиверы/азот на сканирование;
- `power_only.py` — пилот power-only с детектором ложных срабатываний;
- `category_spec_review.py`, `dropped_export.py`, `unmatched_export.py`,
  `match_validation.py` — срезы и валидация.
