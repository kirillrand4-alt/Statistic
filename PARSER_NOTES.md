# Парсер — что улучшить (по данным прогонов 08–10.06)

Анализ: 50 278 уникальных карточек по 6 сайтам. Цена в целом снимается, но
**характеристики и наличие теряются** в нескольких местах. Ниже — конкретика
по сайтам (что чинить и почему это важно для сопоставления товаров).

## Сводка качества

| сайт | URL | цена снята | avail пустой | давление | мощность |
|---|--:|--:|--:|--:|--:|
| compressortyt.ru | 13 911 | 63% | **88%** ⚠ | 74% | 59% |
| pnevmoteh.ru | 9 915 | 43% | 0% ✅ | 98% ✅ | 99% ✅ |
| aerocompressors.ru | 7 745 | 44% | 0% | 66% | 60% |
| rutector.ru | 7 100 | 37% | 2% | 91% | 90% |
| v-p-k.ru | 6 800 | 51% | 2% | **76%** ⚠ | 77% |
| pnevmo-sklad.ru | 4 807 | 42% | 15% | 63% | 63% |

**Эталон качества — pnevmoteh** (ключи с единицами: `мощность, квт` / `давление, бар`
/ `производительность, л/мин`; наличие всегда заполнено). К нему стоит подтянуть остальных.

---

## 1. v-p-k.ru — ГЛАВНЫЙ баг: давление текстом

В поле `Давление` пишется категория **«Среднего» (4771 шт) / «Высокого» (300) / «Низкого»** —
число затирается. При этом число лежит в отдельном поле `Класс давления` / `Давление от-до`.
- **Что делать:** числовое давление в барах брать из правильного поля (где цифры), а
  «Среднего/Высокого» класть в отдельный ключ `Тип давления`. Сейчас на 4771 карточке давление
  как число теряется → товар не сопоставляется по спекам.
- Бонус: у v-p-k есть `Объем ресивера` (1642) — хорошо, но основное поле `Ресивер` бинарное;
  числовой объём приоритетнее.

## 2. compressortyt.ru — наличие и ключ давления

- **`availability` пустой на 88% карточек (12 322 шт).** Цена при этом часто есть, но мы не можем
  отличить «нет в наличии / под заказ / снято» от «парсер не нашёл блок наличия».
  **Что делать:** найти селектор статуса наличия (или, если на странице цена есть и нет пометки —
  ставить `в наличии`; если «под заказ»/«снято» — соответствующее значение).
- Давление лежит под ключом **`Максимальное давление`** (10 066) — это ок, но единица «атм»
  (а не бар); парой встречается мусор `«1-8 бар (регулируемое)»` и `«2.5 атм»`/`«450 атм»`
  (последнее — компрессоры высокого давления, это норма). На моей стороне атм≈бар привожу сам.
- Мощность — `Мощность двигателя` (норм). Значения `0.1 кВт` (76 шт) — это генераторы азота
  (не компрессоры), мой классификатор их отсеивает.

## 3. aerocompressors.ru — единицы и «нет»

- Давление: основной ключ `Давление, бар` ✅, но встречается значение **«нет» (417 шт)** —
  лучше оставлять поле пустым, чем писать «нет» (я это и так отбрасываю).
- Несколько карточек: давление диапазоном `«2,5-5,5»` — возьму max сам.
- **Имена с Excel-битьём (96 шт):** `1.46031.0` вместо `1,9/1,0` — если имя берётся не из `<h1>`
  карточки, брать из `<h1>` (возможно, битьё в самом названии на сайте — тогда не критично,
  я матчу по URL).
- Объём ресивера `Объем ресивера, л` (2586) — отлично, подключил.

## 4. pnevmo-sklad.ru — наличие частично пустое

- `availability` пустой на 15% (728). Аналогично compressortyt — добить селектор наличия.
- Ресивер: `На ресивере` (бинарно, 2833) + `Объем ресивера` (575). Числовой объём — приоритет,
  желательно заполнять чаще.

## 5. rutector.ru / pnevmoteh.ru — почти идеально

- Структура specs ровная, единицы в ключах. Низкий % цены = много «по запросу» (нет публичной
  цены) — это не баг парсера.
- rutector: пара карточек давление списком `«7;8.6;10,3;12»` (несколько моделей на странице) —
  редкость, можно игнорировать.

---

## Приоритеты (по влиянию на сопоставление)

1. **v-p-k: давление числом** (4771 карточка теряет ключевую характеристику) — №1.
2. **compressortyt + pnevmo-sklad: заполнять `availability`** (13 000 карточек без статуса) —
   нужно, чтобы отличать «снято/под заказ» от «парсер не нашёл».
3. Единые единицы: давление в барах (не «атм»/«Среднего»), мощность в кВт, производительность
   в л/мин — как у pnevmoteh. Где есть — числовой **объём ресивера** в литрах.
4. Мелочи (обрабатываю на своей стороне): диапазоны `2,5-5,5`/`1-8`, значение `«нет»`, атм↔бар,
   HP↔kW (в названии модели у Atlas/Ceccato/ABAC число — это л.с., а не кВт).

## Что уже хорошо
- Цена снимается стабильно (дублей URL с конфликтом цен — 0).
- `specs`-JSON валидный, у большинства есть `модель`/`артикул`.
- pnevmoteh заработал (раньше блокировался) и даёт эталонные ключи.
- Новый прогон чище предыдущего: давление с единицами, у aero подтянулся объём ресивера.
