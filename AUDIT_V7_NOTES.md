# Скептичный разбор аудита v7 — промежуточные выводы

## Что нового в v7 (отличие от v6)
- Аудит теперь скормлен МОИМ экспортом `Dropped_nowhere` (тёмная материя) + Unmatched_all.
- Главная новизна — лист **НОВОЕ_Dropped_P1P1B** (1354): пары, где одна из сторон —
  ранее ОТСЕЯННЫЙ до кандидатов URL. Аудит утверждает: 1276 конкурентских «dropped»
  на самом деле матчатся с нашими товарами → значит мои фильтры якобы режут лишнее.
- Вход вырос: наши 15472 (было 7863 — теперь включает dropped-наши), конк 55309.

## КЛЮЧЕВОЙ ВОПРОС
Действительно ли 1276 «dropped» конкурентов — настоящие матчи, которые мой матчер
потерял? Или аудит сматчил мусор (другой бренд / другой тип / запчасть)?
Проверяю по колонке «конк причина» (почему был отсеян) + ручная сверка паспорта.

---
## ВЫВОДЫ

### НОВОЕ_Dropped_P1P1B (1353): бренды
fini 350, dalgakiran 301, comprag 261, zuv 201, ariacom 96, atom 59, airpol 48,
mig 24, boge 8, ir 4, sullair 1. Уникальных наших URL: 627.
Источник наш: 1272 из Dropped/НАШИ, 81 из Unmatched. Причина отсева конк:
1038 «серия+номер не извлекаются», 301 «серии нет у нас».

### ПРИНЯТО (реальные пропуски ser_of) — паттерны:
- **A) HCA+ (Ariacom, 80)** — «+» приклеен к числу (HCA+110), регэксп не ловил.
  → ИСПРАВЛЕНО: добавил `(\+?)` в gen_series, ключ «hca+». Регрессий нет.
- **B) Одно-буквенная серия** (Comprag D-11-08, Dalgakiran F 11-10, IR R110ne,
  Boge S 132-4, Atom А-11) — серия = 1 буква, регэксп требует ≥2 (`{2,12}`).
  Обе стороны идентичны, паспорт сходится. РЕАЛЬНЫЙ пропуск. → надо чинить осторожно.
- **C) Power-only имена** (Airpol 11/10, ZUV 11, MIG 11) — нет слова-серии вообще,
  модель названа по кВт. brand+спеки совпадают 1:1. ОПАСНО авто-мерджить (риск
  склейки разных линеек на одном кВт) — только если ВСЕ спеки (kw+bar+fl) равны.
- **D) Number-first** (Sullair 1150XHADTQ) — цифры перед буквами, регэксп ждёт
  слово+число. Редко (1 шт). Низкий приоритет.
- **E) Fini PLUS** (350 строк!) — «PLUS» это РЕАЛЬНАЯ серия Fini, но «plus» у меня
  в _STOPW (маркетинг). Имена идентичны с обеих сторон. Надо whitelistнуть PLUS
  как серию там, где это первое слово после бренда.

### Скептическая оговорка
Все эти «dropped» пары имеют совпадающий паспорт (kw/bar/fl), поэтому ИСПРАВЛЕНИЕ —
это улучшить ИЗВЛЕЧЕНИЕ серии (ключ-бакет), а корректность добьёт существующий match()
со своими проверками «спеки не противоречат». Но power-only (C) требует политики
матчера, не только экстрактора — там нет серии для различения линеек.

## (старое поле для дозаписи)

## РЕЗУЛЬТАТ (проверено прогоном)
Матчей: 9376 → (v6 кириллица) 10530 → (v7 фиксы) **10916**. Пар 51082.
- HCA+ (Ariacom): извлекается, +матчи. ✓
- Одно-буквенные серии (Atom А, Boge C, Comprag D, Dalgakiran F, IR R): fallback
  только при None в основном проходе. Спот-чек: Atom А-11↔А-11, Boge C4↔C4-10,
  Comprag D-110↔D-110 — паспорт 1:1, легитимно. ✓
- Fini PLUS: «plus» как серия только сразу после бренда; Kraftmann VEGA…PLUS не задет. ✓
- Проверка на монстров: одно-буквенные max=7 сцепок/товар (287 товаров) — НЕТ слияния.
  avg pairs/match у затронутых брендов 1.1–5.1. Берг 16.1 — давнее, не от правок.

## ОГРАНИЧЕНИЯ (не ложные матчи, просто остаются пропущенными)
- Comprag «D-11-08» (наш) vs «D-1108» (конк): номер парсится 11 vs 1108 — не сольются.
  Формат «кВт-бар» через дефис vs склейкой сложно унифицировать генерически. Безопасно
  (ложного матча нет), но требует спец-правила Comprag, если нужно добить.
- Power-only (Airpol 11/10, ZUV 11): серии нет вообще. НЕ трогал — авто-мердж по brand+кВт
  рискует слить разные линейки. Нужна отдельная политика матчера (только при kw+bar+fl
  все равны) — выношу на решение, не делаю молча.

## ПИЛОТ POWER-ONLY (airpol/zuv/mig), правило kw+bar+fl ВСЕ равны
- **airpol: ЧИСТО.** 41/45 матчей, avg 1.1, взаимность 0, токен-диффы тривиальны.
- **mig: 2 ложных паттерна пойманы детекторами:**
  1) наш «MIG 15» и наш «MIG К 15» имеют ОДИНАКОВУЮ физику (15/10/2100) и оба цепляют
     одну карточку конкурента → нужен guard по вариант-токенам (К = другой товар);
  2) конк «MIG 15 PR» (PR=прямой привод) цеплялся к нашему «MIG 15» без маркера →
     вариант-токены должны СОВПАДАТЬ (строго), иначе отклоняем.
- **zuv: наших power-only 0** — производитель в Битриксе у ZUV не распознаётся, бренд
  есть только в названии. Главный пайплайн (load_ours_all) их тоже НЕ видит (205 шт
  вне всех пайплайнов!). Для po-лоадера добавляю фолбэк brand_from_text(name).
  Расширение фолбэка на ВЕСЬ load_ours_all — отдельное решение (риск выше, не делаю молча).
- FIX: po_match + variant_sig (короткие альфа-токены ≤2 после CYR2LAT, без брендов/единиц)
  должны быть РАВНЫ. IP-класс уже сравнивается отдельно.
- Аудит подавал «серия+номер не извлекаются» как доказательство матча, но «наш серия»
  во многих строках = плейсхолдер `compressor_screw` (тип из URL), не реальная серия.
  Это не опровержение находок (паттерны реальны), но скоринг аудита нельзя принимать
  буквально — он матчил по brand+спекам, серию подменял категорией.

## POWER-ONLY: ИТОГ (внедрено)
Пилот после guard'ов: airpol 41 (avg 1.1), mig 11 (avg 1.3, К/PR-ложные отсечены),
zuv 200 (avg 1.0, «прямой/ременной» сравнивается строго). Взаимность 0 у всех.
По ВСЕМ брендам po-матчи дают только 4 бренда (zuv 200, airpol 41, mig 11, sullair 1)
= 253 наших товара. Внедрено: power_only.py + лист «Особо проверить (нет серии)» в
брендовых отчётах. Brands_spec_match.zip пересобран: 51 бренд (+Zuv — он целиком вне
основного пайплайна, бренд только в названии товара).
Прим.: «ременной» в токен-диффах ZUV — ложная тревога самого детектора: наш «ременнЫЙ»
был в стоп-листе диффа, их «ременнОЙ» нет; обе стороны ременные, матчи корректны.

## IP-ФИЛЬТР в основном пайплайне (правило заказчика)
Вопрос «IP23/IP54 всегда сравниваются отдельно?» вскрыл: IP сравнивался ТОЛЬКО в
power-only. В основном спек-матче — нет: 3598 пар с явным конфликтом (наш IP54 ↔ их
IP23/55), 1851 ячейка-клон смешивала разные IP в min-цену (часть «монстра» Berg).
Правило заказчика: 54≈55 — один класс (равны), 23 — другой; НЕ указан ≠ «отличается»
(молчание совместимо с любым). Реализовано: spec_match.ip_class (нормализация 55→54)
+ ip_filter (направленный, режет только явный конфликт обеих сторон). Прокинуто в
load_ours_all/load_comp_all (поле ip) и цепочки brand_spec_review / match_validation /
unmatched_export; power_only переведён на ip_class.
Эффект: матчей 10916→10912 (−4 строки: CrossAir CA…-3GA наш IP54 ↔ их IP23 — честное
расцепление), пар 51082→49428 (−1654 ложных), явных конфликтов осталось 0.
Berg avg 16.1→13.7; спот-чек: наш «ВК-11 16 IP54» цепляет только IP55-карточки v-p-k.
Монстры Berg (34 пары) остались: это НАШИ карточки БЕЗ метки IP (по правилу молчание
не фильтрует) + вариантные суффиксы Р/Е/О — отдельная давняя тема, не IP.

## РАЗБОР «МОНСТРОВ» BERG (вариант-суффиксы) + Zammer /O
Схема Berg ВК расшифрована ПРОПАМИ нашего каталога: Р=ременный привод (проп
«привод»), Е=частотник (проп 22586), О=осушитель (проп 22565); РЕ/РО/РЕО — комбинации.
Дыры: проп «частотник» и «осушитель» вообще не читались лоадером; dr был загружен,
но match() его не сравнивал.
Внедрено:
1) match(): привод сравнивается, если известен с ОБЕИХ сторон (молчание совместимо).
2) berg_suffix() для конкурентов: р→dr, е→vsd, о→ff; отсутствие р → прямой (заводская
   схема кода). Наша сторона — из пропов (vsd-проп пока Berg-only: vsd строгий флаг).
3) Generic: хвост /O|/OW|/O2 → ff (suffix_flags, обе стороны); спек-ключ конкурентов
   «С осушителем: да» → ff (раньше не читался!); наш проп «осушитель=да» → ff глобально.
Подтверждение Zammer /O: спеки конкурентов «С осушителем: да», масса 505 vs 475 кг.
Эффект: пар 49428→37255 (−12173!), матчей 10912→10802. Berg: 8193 пар→~2700, avg
13.7→4.7, семейство ВК-18.5 рассыпалось на 7 корректных вариантов (спот-чек: каждый
цепляет только своих, включая словесные «с ресивером и осушителем» rutector).
dr-расцепления вне Berg честные: ALMiG GEAR ↔ Belt разделились.
Открытые хвосты: ЗИФ-СВЭ кожух/ШР/РН (24 пары) — словесные исполнения, физика та же,
похоже на легитимные клоны; Zammer SKTG11V (возможно Кир. В) слит с базой — вес
идентичен (475кг), доказательств различия нет; глобальный vsd-проп — отдельный замер.

## VSD: финальное правило (заказчик: частотник +35% к цене)
Замеры трёх вариантов: (а) тупое включение пропа — режет живые зеркальные пары
(FORMULA.I↔FORMULA.I, 2243 молчуна); (б) направленная активация — паритет ±3140, но
режет своих в однородных VSD-сериях (pnevmoteh FORMULA.I без спек-ключа);
(в) ПРИНЯТО: есть=есть, нет=нет, «не указано» матчится с особой отметкой.
Знание (приоритет): ИМЯ-маркер > наш проп 22586 / их спек-ключ «частот*» > неизвестно.
Имя надёжнее спек-таблиц т.к. ключ «частот» неоднозначен (Частота тока/вращения/
напряжения ≠ частотный привод). ПОПРАВКА: прежнее «pnevmoteh GENESIS I.15 частотник
нет — враньё» ОШИБОЧНО — pnevmoteh пишет поле корректно (MI/MEI/.I=да, M=нет, инверт.
с «нет» = 0 шт). Вариации написания: VSD/частот/VS/инвертор/inverter +
I-серии ABAC (I.15, FORMULA.I, слаг genesis-i-15) + MEI\d + Berg Е-схема (отсутствие
Е в ВК-коде = знаем-НЕТ). Их спек-ключ: «частот» в ключе со значением да/нет
(«Частота тока: 50» отсеивается значением); «частотн» в значении → да.
match(): тристейт 1/0/None, режем только явный конфликт 1↔0.
Отметка ⚠ только в рисковом случае «одна сторона ДА ↔ вторая молчит» (1539 строк);
«нет↔молчит» не шумим. Итог: матчей 10802→10683, пар 37255→34058 (−3197 ложных
VSD-фикс пар). Контроли: FORMULA.I 3 сайта vsd=1; VARIABLE цепляет только VARIABLE,
GEAR отрезан; ALMiG aerocompressors-молчун жив с пометкой.

## ABAC SPINN FM (репорт заказчика: pnevmoteh spinn-11-fm-10-bar ↔ наш spinn-11-10-fm)
Корень: FM (Floor Mounted, антоним TM) в наших именах стоит ПОСЛЕ давления
(«SPINN 11-10 FM» → ключ spinn), у конкурентов — сразу после числа («SPINN 11 FM 10»
→ ключ spinnfm). Бакеты расходились. Фикс: «fm» в _STOPW (как давно сделанный «tm»);
вариант «на ресивере» дискриминируется rv-флагом+receiver_filter, FM в ключе вреден.
Безопасность: Atlas — свой регэксп (не задет); Fini MED …-FM- — после второго числа
(ключ не менялся). Итог: +11 матчей/+52 пары (10683→10694/34058→34110), потерь 0;
SPINN 11-10 FM собрал все 5 сайтов.

## БАГ is_flow_key: «Производитель» ≠ «Производительность» (нашёлся на запрос HTML rutector)
Запрос заказчика «дай URL где rutector теряет производительность» вскрыл НАШ баг:
specs_kbf / load_comp_all / category(173) / atlas has_flow ловили ключ подстрокой
«произв»/«производ», а он первым попадал на «Производитель» (FIAC), «Производство»,
«Гарантия производителя», «Страна производства». rutector единственный отдаёт ключ
«Производитель» ПЕРЕД «Производительностью» → у него flow=FIAC→None у 3492 карточек.
Фикс: spec_match.is_flow_key() — «производительн»/«пропускн»/«произв*+единица»,
исключая производитель(без -ность)/производство/гарантию; чистит мягкий перенос \xad
(«Производитель­ность»). rutector полный паспорт 2965→6344 (+3379).
Эффект на матчи: 10694→10544 (−150), пар 34110→33644 (−466) — это УЖЕСТОЧЕНИЕ:
rutector раньше матчился без проверки flow (всегда None→permissive), теперь как все
по FLOW_TOL 4%. Из отсеянных −466: 1513 расходятся по flow >15% (честно разные
варианты/расхождение карточек, напр. ABAC SPINN 2.2: наш 240 vs рут 294 л/мин),
1188 в полосе 4-15%. Применён в compressors+categories+need-specs.
Поправка к PARSING_RECOMMENDATIONS: 3 «проблемы парсеров» (rutector flow,
compressortyt kw, compressortyt sku) оказались нашими багами/ложными — парсерам по
ним ничего не делать. Артикул compressortyt есть в спек-ключе «Артикул» (1338 завод.).
