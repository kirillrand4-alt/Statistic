services:
  web:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data        # SQLite database
      - ./secrets:/app/secrets  # GSC service-account JSON, etc.
    restart: unless-stopped
